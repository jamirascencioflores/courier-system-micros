server:
  port: 8089

spring:
  application:
    name: ms-auth
  datasource:
    url: jdbc:postgresql://localhost:5432/db_auth
    username: postgres
    password: Joaquin@3003
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/